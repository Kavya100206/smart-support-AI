Django==4.2.9
djangorestframework==3.14.0
django-cors-headers==4.3.1
psycopg2-binary==2.9.9
dj-database-url==2.1.0
groq==0.4.2
httpx==0.27.2
python-dotenv==1.0.0
gunicorn==21.2.0
whitenoise==6.6.0

# Phase 1: Shopify integration
# (No SDK — we call the Shopify Admin REST API via httpx, which is already
# pulled in for the Groq client. The ShopifyAPI package uses ActiveResource-
# style models that cache response state, conflicting with our
# extract-and-discard rule.)

# Phase 2: Autonomous resolution agent
langgraph==0.2.62
langchain-core==0.3.28
# sentence-transformers is NOT installed in Docker — embeddings are pre-computed
# locally via `python manage.py seed_faqs` and stored as JSON in FAQEntry.embedding.
# Only numpy is needed at runtime for cosine similarity.
numpy==1.26.4

# Phase 3: Reliability + Redis layer
redis==5.2.1
django-redis==5.4.0        # Django cache backend for Redis (settings.CACHES)
django-ratelimit==4.1.0
tenacity==9.0.0

# Testing
pytest==8.3.4              # test runner used directly in all test modules
pytest-django==4.9.0       # bridges pytest + Django: sets DJANGO_SETTINGS_MODULE, test DB, client
