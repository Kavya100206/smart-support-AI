[pytest]
# Always use the test-specific settings which swaps NeonDB for SQLite.
# This eliminates "database is being accessed by other users" teardown errors.
DJANGO_SETTINGS_MODULE = core.settings_test
